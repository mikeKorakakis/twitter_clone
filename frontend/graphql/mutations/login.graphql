mutation LoginUser(
  $email: String!,
  $password: String!
) {
  login(input: {
    email: $email,
    password: $password,
  }) {
    user {
      email
      firstName
      lastName
      displayName
    }
  }
}