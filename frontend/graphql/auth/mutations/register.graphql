mutation Register(
	$input: CreateAccountDto!
) {
	register(
		input: $input
		
	) {
		user {
			email
			firstName
			lastName
			displayName
            image
		}
		error {
			type
			message
		}
	}
}
