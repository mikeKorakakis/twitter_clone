mutation Register(
	$input: CreateAccountDto!
) {
	register(
		input: $input
		
	) {
		user {
            id
			email
			firstName
			lastName
			displayName
            image
		}
		error {
			type
			message
		}
	}
}
